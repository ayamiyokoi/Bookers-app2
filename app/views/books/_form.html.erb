<div>
   <h1>New book</h1>
<!-- form_with部分 -->
  <%= form_with model: new_book, url:'/books', local:true do |f| %>
   <p><%= f.label :Title %></p>
   <%= f.text_field :title %>
   <p><%= f.label :Opinion %></p>
   <%= f.text_area :body %>
   <p><%= f.submit 'Create Book' %></p>
  <% end %>
</div>