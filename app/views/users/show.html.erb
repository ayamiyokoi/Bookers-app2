<% if @user.errors.any? %>
  <%= @user.errors.count %>件のerrorが発生しました
  <% @user.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>
<!--左上部分-->
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
 <%= render 'users/info', new_user: @user %>

<!--左下部分-->
<%= render 'books/form', new_book: @book %>
</div>
<!--右側-->
   <div class="col-md-8 offset-md-1">
  <h1>Books</h1>
  <table class="table table-striped">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>
  <tbody>
   <% @user.books.each do |user1| %>
   <tr>
     <td><%= link_to user_path(user1.user.id) do %><%= attachment_image_tag @user, :profile_image, size: '40x40', fallback: "no_image.jpg" %><% end %></td>
     <td><%= link_to user1.title, book_path(user1.id) %></td>
     <td><%= user1.body %></td>
   </tr>
<% end %>
 </tbody>
 </table>

</div>

  </div>
</div>