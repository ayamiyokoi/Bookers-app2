<%= render 'users/info', new_user: @user %>
<%= render 'books/form', new_book: @book %>
<h1>Users</h1>
<% @users.each do |user| %>
  <p>name</p>
  <span><%= user.name %></span>
  <%= link_to("Show", user_path(user)) %>
<% end %>